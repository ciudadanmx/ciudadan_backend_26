'use strict';

/**
 * codigosreferido controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::codigosreferido.codigosreferido');
